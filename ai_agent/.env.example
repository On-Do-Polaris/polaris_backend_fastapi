# ============================================================
# SKAX Physical Risk Analyzer - Environment Variables
# ============================================================
# Copy this file to .env and fill in your actual values
# Updated: 2025-12-17

# ============================================================
# 🔴 REQUIRED - 필수 설정 (없으면 앱 실행 불가)
# ============================================================

# OpenAI API Key (LLM 호출용)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# Application Database URL (PostgreSQL)
# 형식: postgresql://user:password@host:port/database
APPLICATION_DATABASE_URL=postgresql://user:password@localhost:5432/app_db

# Data Warehouse Database URL (PostgreSQL)
# 형식: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://user:password@localhost:5432/dw_db

# ============================================================
# 🟡 RECOMMENDED - 권장 설정 (없으면 기능 제한)
# ============================================================

# Qdrant Vector DB URL (RAG 검색용)
# 로컬 개발: http://localhost:6333
# GCP: https://your-qdrant-instance.region.gcp.cloud.qdrant.io
QDRANT_URL=http://localhost:6333
QDRANT_API_KEY=  # Qdrant Cloud 사용 시 필요
QDRANT_COLLECTION=esg_tcfd_reports

# LangSmith (LLM 트레이싱/모니터링)
# Get from: https://smith.langchain.com/settings
LANGSMITH_ENABLED=false
LANGSMITH_API_KEY=ls__...
LANGSMITH_PROJECT=tcfd-report

# ============================================================
# 🟢 OPTIONAL - 선택 설정 (기본값 사용 가능)
# ============================================================

# 실행 환경
# 옵션: development, production, test
ENVIRONMENT=development

# 디버그 모드
DEBUG=true

# 로깅 레벨
# 옵션: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# OpenAI Rate Limit 설정
OPENAI_RPM=60       # 분당 요청 수 (기본: 60)
OPENAI_TPM=150000   # 분당 토큰 수 (기본: 150000)

# RAG 설정
RAG_MOCK_MODE=false  # true: Mock 데이터 사용 (테스트용)

# Workflow Mock 모드
WORKFLOW_MOCK_MODE=false

# ============================================================
# 🔧 GCP Cloud Run 배포 설정
# ============================================================

# Cloud Run은 PORT 환경변수를 자동 주입
PORT=8080

# Spring Boot 서버 URL (백엔드 연동)
SPRING_BOOT_BASE_URL=http://localhost:8080

# ModelOps 서버 URL
MODELOPS_BASE_URL=http://localhost:8001

# ============================================================
# 📁 Scratch Space 설정 (임시 파일 저장)
# ============================================================

SCRATCH_BASE_PATH=./scratch
SCRATCH_TTL_HOURS=4
SCRATCH_AUTO_CLEANUP=true
SCRATCH_CLEANUP_INTERVAL=1
SCRATCH_MAX_SIZE_MB=10240

# ============================================================
# 🔒 GCP Secret Manager (프로덕션 환경)
# ============================================================
# GCP Secret Manager 사용 시 아래 형식으로 설정
# OPENAI_API_KEY=projects/PROJECT_ID/secrets/openai-api-key/versions/latest
#
# Cloud Run에서는 Secret Manager 연동 설정:
# gcloud run deploy SERVICE_NAME \
#   --set-secrets=OPENAI_API_KEY=openai-api-key:latest
