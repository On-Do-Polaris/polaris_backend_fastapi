# 물리적 리스크 보고서 구조 명세서

**작성일**: 2025-12-15
**버전**: v1.0
**목적**: 물리적 기후 리스크 시나리오 분석 보고서의 데이터 구조 및 워크플로우 정의

---

## 📋 개요

### 보고서 범위
- ✅ **포함**: 물리적 기후 리스크 시나리오 분석 (SSP 기반)
- ❌ **제외**: TCFD 전체 프레임워크 (거버넌스, 전환 리스크 등)

### 핵심 컴포넌트
1. **시나리오 분석**: SSP 1-2.6, 2-4.5, 3-7.0, 5-8.5
2. **시점 분석**: 단기(2026), 중기(2026-2030), 장기(2020-2050년대)
3. **리스크 평가**: 만성 리스크 2종 + 급성 리스크 7종
4. **사업장별 표**: 각 사업장당 독립적인 리스크 매트릭스 표

---

## 🕐 시간 구간 정의

### 1. 단기 (Short-term)
```json
{
  "label": "단기 (향후 1년)",
  "years": [2026],
  "granularity": "1년 단위"
}
```

### 2. 중기 (Mid-term)
```json
{
  "label": "중기 (향후 5년)",
  "years": [2026, 2027, 2028, 2029, 2030],
  "granularity": "1년 단위"
}
```

### 3. 장기 (Long-term)
```json
{
  "label": "장기 (2050년까지)",
  "periods": ["2020년대", "2030년대", "2040년대", "2050년대"],
  "granularity": "10년 단위 평균"
}
```

**⚠️ 주의**: 2100년 데이터는 보고서에 포함하지 않음

---

## 🌍 SSP 시나리오

### SSP 1-2.6 (Net Zero)
- **목표**: 2050년까지 Net Zero 달성
- **온도 상승**: 2100년까지 1.3~2.4°C
- **특징**: 파리 협정 목표 달성 시나리오

### SSP 2-4.5
- **목표**: 2050년까지 현재 수준에서 안정, 이후 감소
- **온도 상승**: 2100년까지 2.1~3.5°C
- **특징**: 중간 배출 시나리오

### SSP 3-7.0
- **목표**: 현재 추세 지속
- **온도 상승**: 2100년까지 3.0~4.5°C
- **특징**: 기준 시나리오 (Business as Usual)

### SSP 5-8.5 (최고 배출)
- **목표**: 화석 연료 중심 경제 지속
- **온도 상승**: 2100년까지 4.0~5.5°C
- **특징**: 최악의 시나리오

---

## 📊 리스크 카테고리

### 만성 리스크 (Chronic Risks)

| Risk Type | Risk Name | Description |
|-----------|-----------|-------------|
| `temperature_change` | 이상기온 | 장기적 기온 상승으로 인한 냉방 수요 증가, 설비 과부하 |
| `sea_level_rise` | 해수면 상승 | 해안 지역 침수 위험 증가 (내륙 사업장은 영향 없음) |

### 급성 리스크 (Acute Risks)

| Risk Type | Risk Name | Description |
|-----------|-----------|-------------|
| `urban_flood` | 우수 홍수 | 도심 집중호우로 인한 침수 |
| `river_flood` | 하천 홍수 | 하천 범람으로 인한 침수 |
| `coastal_flood` | 해안 홍수 | 해안 지역 고조 및 폭풍해일 |
| `drought` | 가뭄 | 수자원 부족으로 인한 운영 중단 |
| `wildfire` | 산불 | 산불 확산으로 인한 피해 |
| `typhoon` | 태풍 | 강풍 및 호우로 인한 구조 손상 |
| `water_stress` | 물부족 | 장기적 수자원 스트레스 |

---

## 🎨 리스크 레벨 정의

| Level | Label | Color | AAL Range (%) | Description |
|-------|-------|-------|---------------|-------------|
| `none` | - | `#D3D3D3` | 0 | 리스크 없음 |
| `very_low` | 0~3% | `#FFF4CC` | 0 ~ 3 | 매우 낮음 |
| `low` | ~6% | `#D4E8C1` | 3 ~ 6 | 낮음 |
| `medium` | ~10% | `#A8D57E` | 6 ~ 10 | 보통 |
| `high` | ~16% | `#FDB885` | 10 ~ 16 | 높음 |
| `very_high` | ~30% | `#F59547` | 16 ~ 30 | 매우 높음 |
| `extreme` | 30%~ | `#C84D4D` | 30+ | 극심 |

**AAL (Average Annual Loss)**: 연평균 자산 손실률

---

## 📈 보고서 데이터 구조

### 전체 JSON 스키마

```json
{
  "report_metadata": {
    "company_name": "string",
    "report_title": "string",
    "report_date": "YYYY-MM-DD",
    "reporting_period": "string"
  },

  "time_horizons": {
    "short_term": {...},
    "mid_term": {...},
    "long_term": {...}
  },

  "scenarios": [...],

  "risk_categories": {
    "chronic": {...},
    "acute": {...}
  },

  "risk_levels": {...},

  "sites_risk_assessment": [
    {
      "site_id": int,
      "site_name": "string",
      "site_type": "string",
      "address": "string",
      "location": {
        "latitude": float,
        "longitude": float
      },
      "risk_table": {
        "SSP1-2.6": {
          "chronic_risks": {
            "temperature_change": {
              "2020": {
                "aal_percent": float,
                "risk_level": "string",
                "color": "string"
              },
              "2030": {...},
              "2040": {...}
            },
            "sea_level_rise": {...}
          },
          "acute_risks": {
            "urban_flood": {...},
            "river_flood": {...},
            "coastal_flood": {...},
            "drought": {...},
            "wildfire": {...},
            "typhoon": {...},
            "water_stress": {...}
          }
        },
        "SSP2-4.5": {...},
        "SSP3-7.0": {...},
        "SSP5-8.5": {...}
      },
      "vulnerability_summary": {
        "high_risk_factors": [...],
        "resilience_factors": [...]
      }
    }
  ],

  "summary": {
    "key_findings": [...],
    "overall_risk_trend": "string"
  }
}
```

---

## 🔄 워크플로우 구조

### 전체 파이프라인

```
┌─────────────────────────────────────────┐
│  Primary Data Agents (사전 실행)          │
│  ├─ BuildingCharacteristicsAgent (BC)   │
│  └─ AdditionalDataAgent (AD)            │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│  Node 0: 데이터 수집                      │
│  ├─ DB에서 사업장 정보 조회                │
│  ├─ ModelOps 물리적 리스크 점수 조회       │
│  └─ BC/AD 결과 통합                       │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│  Node 1: 사업장 분석                      │
│  (현재 구조 유지)                          │
└─────────────────────────────────────────┘
              ↓
┌──────────────────────────────────────────────────────┐
│  Node 2-A: Scenario Analysis                        │
│  - SSP 1-2.6/2-4.5/3-7.0/5-8.5 시나리오 분석         │
│  - 리스크 타입별 AAL 계산                             │
│  - ❌ 표 생성 안함 (데이터만 생성)                     │
│  - ❌ 하드코딩 제거                                   │
└──────────────────────────────────────────────────────┘
              ↓
┌──────────────────────────────────────────────────────┐
│  Node 2-B: Impact Analysis                          │
│  - 사업장별 영향 분석                                 │
│  - 취약점 식별                                        │
│  - ❌ 표 생성 안함 (데이터만 생성)                     │
│  - ❌ 하드코딩 제거                                   │
└──────────────────────────────────────────────────────┘
              ↓
┌──────────────────────────────────────────────────────┐
│  Node 2-C: Mitigation Strategies                    │
│  - 대응 전략 도출                                     │
│  - 우선순위 결정                                      │
│  - ❌ 표 생성 안함 (데이터만 생성)                     │
│  - ❌ 하드코딩 제거                                   │
└──────────────────────────────────────────────────────┘
              ↓
┌──────────────────────────────────────────────────────┐
│  Node 3: 통합 + 표 생성 (Strategy Section)          │
│  - Node 2-A/B/C 결과 통합                            │
│  - ✅ 사업장별 리스크 표 생성 (JSON)                  │
│  - SSP 시나리오 × 시점 × 리스크 매트릭스             │
│  - 색상 코딩 적용 (risk_level → color)               │
│  - 최종 sites_risk_assessment 구조화                │
└──────────────────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│  Node 4/5/6: Validation/Refine/Finalize │
│  (현재 구조 유지)                          │
└─────────────────────────────────────────┘
```

---

## 🎯 Node별 역할 상세

### Primary Data Agents (사전 실행)
**실행 시점**: Node 0 이전
**입력**: 사업장 정보 (DB에서 조회)
**출력**:
- BuildingCharacteristicsAgent: 건물 취약성/회복력 가이드라인
- AdditionalDataAgent: Excel 기반 추가 데이터 가이드라인

---

### Node 0: 데이터 수집
**입력**: API 요청 (사업장 ID 리스트)
**처리**:
1. DB에서 사업장 정보 조회 (좌표, 주소, 건물 정보)
2. ModelOps에서 물리적 리스크 점수 조회 (9개 리스크 타입)
3. Primary Data Agents 결과 로드
**출력**:
```python
{
  "sites": [...],  # 사업장 목록
  "risk_scores": {...},  # ModelOps 결과
  "building_characteristics": {...},  # BC Agent 결과
  "additional_data": {...}  # AD Agent 결과
}
```

---

### Node 1: 사업장 분석
**입력**: Node 0 출력
**처리**: 사업장별 기본 분석
**출력**: 사업장별 분석 결과
**변경 사항**: 없음 (현재 구조 유지)

---

### Node 2-A: Scenario Analysis
**입력**: Node 1 출력
**처리**:
1. 4개 SSP 시나리오별 분석
2. 시점별 (2020, 2030, 2040) 리스크 계산
3. 9개 리스크 타입별 AAL 산출
**출력**:
```python
{
  "scenarios": {
    "SSP1-2.6": {
      "temperature_change": {
        "2020": {"aal_percent": 1.5},
        "2030": {"aal_percent": 2.1},
        "2040": {"aal_percent": 2.8}
      },
      # ... 다른 리스크 타입
    },
    # ... 다른 시나리오
  }
}
```
**제거**: ❌ 표 생성 로직, ❌ 하드코딩된 텍스트

---

### Node 2-B: Impact Analysis
**입력**: Node 2-A 출력
**처리**:
1. 사업장별 영향 분석
2. 고위험 요소 식별
3. 취약점 평가
**출력**:
```python
{
  "impacts": {
    "site_1": {
      "high_risk_factors": [...],
      "vulnerability_score": float
    }
  }
}
```
**제거**: ❌ 표 생성 로직, ❌ 하드코딩된 텍스트

---

### Node 2-C: Mitigation Strategies
**입력**: Node 2-B 출력
**처리**:
1. 대응 전략 도출
2. 우선순위 결정
3. 투자 계획 제안
**출력**:
```python
{
  "strategies": {
    "site_1": {
      "priority_actions": [...]
    }
  }
}
```
**제거**: ❌ 표 생성 로직, ❌ 하드코딩된 텍스트

---

### Node 3: 통합 + 표 생성 (핵심 변경)
**입력**: Node 2-A/B/C 모든 결과
**처리**:
1. **데이터 통합**: 시나리오, 영향, 전략 데이터 병합
2. **risk_level 계산**: aal_percent → risk_level + color 매핑
3. **표 JSON 생성**: 사업장별 독립적인 `risk_table` 생성
4. **최종 구조화**: `sites_risk_assessment` 배열 구성

**출력 예시**:
```python
{
  "sites_risk_assessment": [
    {
      "site_id": 1,
      "site_name": "대덕 데이터센터",
      "risk_table": {
        "SSP1-2.6": {
          "chronic_risks": {
            "temperature_change": {
              "2020": {
                "aal_percent": 1.5,
                "risk_level": "very_low",
                "color": "#FFF4CC"
              },
              # ...
            }
          },
          "acute_risks": {...}
        },
        # ... 다른 시나리오
      }
    },
    {
      "site_id": 2,
      "site_name": "판교 데이터센터",
      "risk_table": {...}
    }
  ]
}
```

**핵심 로직**:
```python
def calculate_risk_level(aal_percent: float) -> tuple[str, str]:
    """AAL % → (risk_level, color)"""
    if aal_percent == 0:
        return "none", "#D3D3D3"
    elif aal_percent <= 3:
        return "very_low", "#FFF4CC"
    elif aal_percent <= 6:
        return "low", "#D4E8C1"
    elif aal_percent <= 10:
        return "medium", "#A8D57E"
    elif aal_percent <= 16:
        return "high", "#FDB885"
    elif aal_percent <= 30:
        return "very_high", "#F59547"
    else:
        return "extreme", "#C84D4D"
```

---

## 📄 최종 보고서 섹션

### 1. Executive Summary
- 핵심 발견사항
- 전체 리스크 수준 요약

### 2. 물리적 리스크 개요
- SSP 시나리오 설명
- 분석 방법론

### 3. 사업장별 리스크 평가 (표)
- **사업장 1**: 대덕 데이터센터
  - 리스크 매트릭스 표 (4 시나리오 × 3 시점 × 9 리스크)
  - 취약성 요약
- **사업장 2**: 판교 데이터센터
  - 리스크 매트릭스 표
  - 취약성 요약

### 4. 종합 분석
- 전체 리스크 트렌드
- 시나리오별 비교

### 5. 대응 전략
- 우선순위 액션
- 투자 계획

---

## 🛠️ 구현 체크리스트

### Phase 1: Node 구조 수정
- [ ] Node 2-A: 하드코딩 제거, 표 생성 로직 삭제
- [ ] Node 2-B: 하드코딩 제거, 표 생성 로직 삭제
- [ ] Node 2-C: 하드코딩 제거, 표 생성 로직 삭제
- [ ] Node 3: 표 생성 로직 추가

### Phase 2: 표 생성 로직 구현
- [ ] `calculate_risk_level()` 함수
- [ ] `generate_risk_table()` 함수 (사업장별)
- [ ] `sites_risk_assessment` 구조 생성

### Phase 3: State 정의 업데이트
- [ ] `TCFDState` 클래스 수정
- [ ] `sites_risk_assessment` 필드 추가
- [ ] `risk_table` 스키마 정의

### Phase 4: 테스트
- [ ] Node 2-A/B/C 단위 테스트
- [ ] Node 3 표 생성 테스트
- [ ] 전체 워크플로우 통합 테스트
- [ ] 실제 데이터로 E2E 테스트

---

## 📚 참고 문서

- [Primary Data Agents Gap Analysis](../planning/primary_data_agents_gap_analysis.md)
- [TCFD Report v2.1 Overview](./tcfd_v2.1_overview.md)
- [Database Schema (DBML)](../database/schema.dbml)

---

**최종 업데이트**: 2025-12-15
**작성자**: AI Agent Team
