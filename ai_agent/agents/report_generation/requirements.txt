# Python
python>=3.11

# LLM 호출 및 비동기 지원
openai>=1.0.0
httpx>=0.24.0
aiohttp>=3.9.0
asyncio

# 데이터 처리
pydantic>=2.0
numpy>=1.25
pandas>=2.1

# Markdown / 문서 변환
markdown2>=2.4.2
pdfkit>=1.0.0
python-docx>=0.8.12

# PDF 변환용 백엔드
wkhtmltopdf
# 또는 pandoc, xelatex 설치 필요 (시스템 패키지)

# 데이터베이스 연결 (Optional, FinalizerNode)
sqlalchemy>=2.0

# Logging
loguru>=0.7.0

# 벡터 DB / RAG
qdrant-client>=1.2.0
tavily-client>=0.1.0

# 기타 유틸
python-dotenv>=1.0.0  # 환경변수 로딩
requests>=2.31.0
